[package]
name = "pw-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

# The versioned browser↔server application protocol.
#
# It depends on the two IDENTITY crates and on neither endpoint's
# implementation. A server and a browser runtime both read it; neither owns it.

[dependencies]
pw-document = { path = "../pw-document" }
pw-resource = { path = "../pw-resource" }
serde.workspace = true
serde_json.workspace = true
