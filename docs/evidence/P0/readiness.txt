P0 readiness — recomputed after E2B, E2C and E2D
recorded: 2026-08-06T14:31:00Z
commit:   936f2d1
measured: docs/evidence/E2D/corpus-enforcement.txt, from `just evidence-corpus`
          (written by the same test the ratchet asserts on, so the published
          number cannot drift from the enforced one)

================= THE GATE ======================================
  At least 40 of 44 rejected corpus programs are source-level
  compile errors, each failing for its declared invariant.

================= WHERE IT STANDS ==============================
  enforced:      33 / 44
  wrong reason:   0
  gate:          40 / 44
  gap:            7

  The previous recomputation (b993896) predicted ~34 after finishing
  E2B member lookup and extending E2D. The actual figure is 33. The
  one it over-counted was R-006, which needs privacy flow, not
  effect inference.

================= WHAT THE REMAINING 11 NEED ===================

  E5 — privacy sinks and capability at a boundary          3
    R-006  a `Secret<Payments>` value reaching `log<Public>`.
           E2D infers the effect and the label algebra exists;
           `log<Public>` is not modelled as a sink the label may
           not cross into.
    R-024  raw HTML without the capability that permits it.
    R-025  `device.current_location()` under `placement origin`.
           NOT a missing analysis. The rule exists and is
           registered (PW5005, rules.rs); it reads the DECLARED
           effect row, and R-025's query declares none. It needs
           two connections, both already-open E2B/E2C items:
             1. a `device` platform module, so the call resolves;
             2. the placement rule consuming INFERRED effects
                rather than only declared ones.

  E9 — type checking                                       3
    R-008  an ambient null assumption (`Option<Store>` vs `null`)
    R-009  an unchecked external cast
    R-022  a form handler whose type does not match its form

  E9C — affine and linear resources                        2
    R-011  a transaction still open on one return path
    R-012  a `use`-bound handle escaping into module state

  E7 — serialization and the resume manifest               2
    R-010  a handler capturing something not serializable
    R-030  private data crossing into the public resume manifest

  E6 — whole-program reachability                          1
    R-023  a route nothing can reach

================= THE ARITHMETIC, HONESTLY =====================

  33 now. The gate needs 7 more.

    E5 alone            -> 36
    E5 + E9             -> 39     still short
    E5 + E9 + any one of E9C / E7 / E6  -> 40, 41 or 42

  So reaching the P0 gate requires AT LEAST THREE more milestones,
  and no two of them suffice. This has not changed in character
  since the last recomputation — only in size, from 20 to 7.

================= WHAT E2B/E2C/E2D ACTUALLY BOUGHT =============

  20 -> 33 enforced, and zero wrong-reason catches at every step.

  The three numbers moved together throughout: at no point did the
  count rise without the diagnostics also naming the declared
  invariant. Two occasions where they diverged were treated as
  regressions and fixed rather than banked:

    25 -> 27  when effect identity kept its type argument,
              `secret<Payments>` stopped covering `secret.read` and
              two fixtures were reported for effects they HAD
              declared. Corrected back to 25.
    R-012     was caught, but for a second defect that masked its
              declared invariant. The fixture was repaired, not the
              count.

  Four fixtures were uncaught because a call resolved to nothing
  rather than because a rule was missing: R-001 (`fetch_store`),
  R-041 (`document`), and R-004/R-012 (missing imports). Each was
  repaired by giving the fixture the declaration it calls. R-025 is
  the same shape and is NOT repaired, because its second half is a
  real E5 connection rather than a missing import.

================= THEREFORE ====================================

  P0 IS NOT PUBLISHABLE. docs/EVIDENCE_LEDGER.md governs every
  public claim; no landing page may imply otherwise.

  What may be said today, and is measured:
    - 33 of 44 rejected corpus programs are compile errors in real
      `.pw` source, each failing for the invariant it declares.
    - All 24 accepted programs check clean.

  What may NOT be said:
    - that the corpus is enforced, or broadly enforced;
    - any figure above 33/44;
    - that effect inference is complete — it is complete for the
      cases the corpus specifies, and the placement solver and the
      privacy flow still read declared rows rather than inferred
      ones.
