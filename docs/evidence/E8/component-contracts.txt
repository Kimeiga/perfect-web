E8-0 — the compiler→host ComponentContract for the store demo

produced by: just e8-contracts

One contract per DECLARATION, not per module: least authority, and a new
declaration leaves every existing contract byte-identical.

Note StorePage: it requires session.read — it builds a query key from
the session, so it reads the session to RENDER — and it does NOT
require database.write<Carts>, which its deferred handler performs.
Authority is recorded once, against the thing that performs it.

This note said rendering needs no authority and runs anywhere until
2026-08-10. It was true of a page whose current_session() call named
nothing. See docs/CORPUS.md C5 and tests/unresolved_provenance.rs.

Resources.Cart  abi 65933ef1a76eff17
  runs at   build, browser, edge, origin
  exports   query Cart

Resources.Menu  abi 295b1410021b7731
  runs at   build, browser, edge, origin
  exports   query Menu

Resources.Order  abi 74d8f0d6d1bf67c7
  runs at   build, browser, edge, origin
  exports   query Order

Resources.Store  abi 67b9938482d95947
  runs at   build, browser, edge, origin
  exports   query Store

store.page.Cart  abi 37cb9e8b9dc1738c
  runs at   origin
  needs     database.read<Carts>
  host      pw:host/database#read  (database.read<Carts>)
  exports   query Cart

store.page.Menu  abi 60058db28f2ab607
  runs at   origin
  needs     database.read<Menus>
  host      pw:host/database#read  (database.read<Menus>)
  exports   query Menu

store.page.Store  abi 4aedcc2febb0861f
  runs at   origin
  needs     database.read<Stores>
  host      pw:host/database#read  (database.read<Stores>)
  exports   query Store

store.page.StorePage  abi d803b26130c7cf49
  runs at   origin
  needs     session.read
  component pw:app/store.page.Cart#Cart
  component pw:app/store.page.Menu#Menu
  component pw:app/store.page.Store#Store
  component pw:app/store.page.add_to_cart#add_to_cart
  component pw:app/store.page.clear_cart#clear_cart
  host      pw:host/session#read  (session.read)
  exports   page StorePage

store.page.add_to_cart  abi 667b06541a47fc42
  runs at   origin
  needs     database.write<Carts>
  needs     session.read
  host      pw:host/database#write  (database.write<Carts>)
  host      pw:host/session#read  (session.read)
  exports   command add_to_cart

store.page.clear_cart  abi a3bcfeb6f0683204
  runs at   origin
  needs     database.write<Carts>
  needs     session.read
  host      pw:host/database#write  (database.write<Carts>)
  host      pw:host/session#read  (session.read)
  exports   command clear_cart

