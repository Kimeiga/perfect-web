// Generated by pw: resumable handler e1ab9fca1f6fc15b. Do not edit.

function trap(what) {
  throw new Error("trap: " + what);
}

// An Int sent to a command as a JSON number (ADR-0058): exact within
// ±2^53, and a trap past it rather than a different number.
function exact(v) {
  if (v < -(2n ** 53n) || v > 2n ** 53n) trap("an Int a JavaScript number cannot carry exactly");
  return Number(v);
}

export const name = "add_to_cart";
export const handler = "e1ab9fca1f6fc15b";
export async function run(context) {
  const v0 = context.captures["item"]["id"];
  const v1 = 1n;
  const v2 = v1;
  const v3 = await context.command("store.page.add_to_cart", [v0, exact(v2)]);
  return v3;
}
