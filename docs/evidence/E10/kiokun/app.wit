// Generated by `pw emit-wit`. Do not edit.
//
// One world per ComponentContract. A world's imports are exactly the
// contract's imports, projected onto interfaces — see pw-core/src/wit.rs.
//
// The host packages are HERE, generated, because the Pleris declaration is
// the ABI authority for every operation this program declares. Architect
// ruling, 2026-08-20: a deployment IMPLEMENTS this interface; it does not
// independently specify what the interface means. One interface has one
// ABI authority, never a Pleris signature plus a hand-authored WIT one
// with a comparison keeping them synchronized.

package pw:app@0.1.0;

interface kiokun-page-lookup-api {
    use pw:types/types.{dictionary-entry};
    lookup: func(arg0: string) -> option<dictionary-entry>;
}

interface kiokun-page-search-api {
    use pw:types/types.{dictionary-search-hit};
    search: func(arg0: string) -> list<dictionary-search-hit>;
}

interface kiokun-page-search-page-api {
    use pw:types/types.{dictionary-search-hit};
    search-page: func(arg0: string, arg1: list<dictionary-search-hit>);
}

interface kiokun-page-word-page-api {
    use pw:types/types.{dictionary-entry};
    word-page: func(arg0: string, arg1: option<dictionary-entry>);
}

interface shards-place-api {
    use pw:types/types.{dictionary-placement};
    place: func(arg0: string) -> dictionary-placement;
}

interface shards-places-api {
    use pw:types/types.{dictionary-placement};
    places: func(arg0: list<string>) -> list<dictionary-placement>;
}

world kiokun-page-lookup {
    import kiokun:data/entries;
    export kiokun-page-lookup-api;
}

world kiokun-page-search {
    import kiokun:data/index;
    export kiokun-page-search-api;
}

world kiokun-page-search-page {
    export kiokun-page-search-page-api;
}

world kiokun-page-word-page {
    export kiokun-page-word-page-api;
}

world shards-place {
    export shards-place-api;
}

world shards-places {
    export shards-places-api;
}

package pw:types {
    interface types {
        record dictionary-chinese-word {
            id: string,
            traditional: string,
            simplified: string,
            pinyin: string,
            definitions: string,
        }
        record dictionary-japanese-sense {
            id: string,
            glosses: string,
        }
        record dictionary-japanese-word {
            id: string,
            written: string,
            readings: string,
            senses: list<dictionary-japanese-sense>,
            common: bool,
        }
        record dictionary-korean-word {
            id: string,
            hangul: string,
            hanja: string,
            pronunciation: string,
            definitions: string,
        }
        record dictionary-japanese-name {
            id: string,
            written: string,
            readings: string,
            kinds: string,
            translations: string,
        }
        record dictionary-character {
            strokes: option<s64>,
            grade: option<s64>,
            jlpt: option<s64>,
            frequency: option<s64>,
            hangul: string,
            meanings: string,
        }
        record dictionary-entry {
            key: string,
            redirect: option<string>,
            chinese: list<dictionary-chinese-word>,
            japanese: list<dictionary-japanese-word>,
            korean: list<dictionary-korean-word>,
            names: list<dictionary-japanese-name>,
            character: option<dictionary-character>,
        }
        record dictionary-search-hit {
            id: string,
            word: string,
            target: string,
            language: string,
            pronunciation: string,
            definition: string,
            common: bool,
        }
        record dictionary-placement {
            shard: string,
            subdirectory: string,
        }
        record dictionary-row {
            word: string,
            target: string,
            language: string,
            pronunciation: string,
            reading: string,
            definition: string,
            folded: string,
            words: list<string>,
            common: bool,
        }
    }
}

package kiokun:data {
    interface entries {
        use pw:types/types.{dictionary-entry};
        get: func(arg0: string) -> option<dictionary-entry>;
    }
    interface index {
        use pw:types/types.{dictionary-row};
        alias: func(arg0: string) -> option<string>;
        candidates: func(arg0: string) -> list<dictionary-row>;
        fold: func(arg0: string) -> string;
    }
}

