# E3 — Marko rendering adapter, streaming SSR, first resumption

**Status: COMPLETE.** All six gate items pass.

## Question

> Can the custom language produce a modern streamed and resumable HTML
> application before building a renderer from scratch?

**Answered: yes.** Three applications written entirely in `.pw` generate Marko,
build, serve, stream, and pass in three browser engines.

## Deliverables

| deliverable | where |
|---|---|
| Markup as a syntax tree, not a token run | `compiler/pw-syntax/src/grammar.rs` |
| Markup in HIR: elements, attributes, text | `compiler/pw-core/src/hir.rs` |
| The adapter (ADR-0017) | `compiler/pw-core/src/marko.rs` |
| `pw emit-marko --out DIR` | `compiler/pw-cli` |
| `examples/hello-static`, `examples/counter`, `examples/streamed` | `examples/` |
| Generate → build → measure → browsers | `spikes/pw-to-marko/run.sh` |

Evidence: `docs/evidence/E3/spike-pw-to-marko.txt`. Reproduce with
`just spike-pw-to-marko`.

## Measured

All three routes are generated from `.pw`; nothing under `src/routes` is
authored. `src/resources.mjs` is hand-written host code, and is the only
hand-written thing the routes depend on (ADR-0018).

| route | HTML | gzip | `<script src>` | inline | client JS | gzip |
|---|---|---|---|---|---|---|
| `/static` | 346 B | 222 B | **0** | **0** | **0 B** | **0 B** |
| `/counter` | 1,109 B | 672 B | 1 | 393 B | 4,314 B | 2,300 B |
| `/streamed` | 1,372 B | 833 B | **0** | 889 B | **0 B** | **0 B** |

Streaming timing, read chunk by chunk from the response:

```text
transfer-encoding   chunked
chunks              2
SHELL_READY            4 ms
"Finding recommendations"  4 ms    ← the placeholder
"Affogato"          1205 ms        ← the 1200 ms region
```

The shell was usable **1,201 ms before** the slow region arrived. Byte counts
cannot answer this — a response that buffers everything and sends it at the end
has identical bytes — so the only evidence is *when* each marker arrives.

The static route ships **no JavaScript at all** — no script tag, no request.
That is the byte-level item M3 asks for, met from `pw` source rather than from
a hand-written template.

The counter ships 4.3 kB for one button on a page that is otherwise inert
markup. That is the charter §8.5 claim the measurement exists to test, and it
is Marko's behaviour measured through `pw` (ADR-0017), not `pw`'s own.

## Gate

Charter §14 M3 lists six items.

| # | gate item | result |
|---|---|---|
| 1 | Static page usable with JavaScript disabled | **PASS** — asserted with JS actually off, in all three engines |
| 2 | Static page emits zero application JS, ideally zero framework JS | **PASS** — zero of both; no script tag and no JS request |
| 3 | Delayed content streams without blocking the shell | **PASS** — shell at 4 ms, region at 1,205 ms, two chunks; asserted in the browser too, where the placeholder must be visible while the region is absent |
| 4 | Counter interaction works without whole-page re-execution | **PASS** — a server-rendered node is marked, the counter is clicked twice, and the marker survives |
| 5 | All three Playwright engines pass | **PASS** — 30 tests × Chromium, Firefox, WebKit |
| 6 | Generated code is not exposed in normal diagnostics | **PASS** — carried over from E2 (`explain_never_leaks_generated_paths`); `pw check` never names a generated file |

### How item 3 was closed

It was blocked on `pw` having no way to name a value that is not ready yet. The
corpus already specified the syntax — `A-008` writes
`<stream query={Recommendations(id)}>` with `<placeholder>`, `<ready as={..}>`
and `<failed as={..}>` — so the work was to make the language and the adapter
carry it, not to invent it:

1. **Markup blocks nest.** `{#each ..}` and `{/each}` were siblings in the
   syntax tree, so a renderer could not tell what was inside the loop. They now
   nest exactly like elements.
2. `{#each xs as x (k)}` becomes `<for|x| of=xs by="k">`. Marko's `by` takes a
   property *name* for objects and a *function* for primitives, and the loop
   parameter is not in scope inside `by=` — `by=item.id` compiles and silently
   re-keys the list on every render.
3. `<stream>` becomes `<try>` / `<await>`, with `@placeholder` and `@catch` on
   the `<try>` and never on the `<await>` (E0 finding F-5).
4. A template that calls a query imports its implementation from a
   host-provided module. The compiler says *how* a resource behaves via the
   manifest (ADR-0018); the host decides *what* it does.

## What implementing it found

**Markup was a token run.** It round-tripped and parsed the whole corpus and
was useless for rendering. Losslessness was never the hard part; structure was.

**Whitespace between inline elements is content.** The parser treated it as
trivia, so it never reached HIR and `<span>Espresso</span> <span>$3.50</span>`
rendered as `Espresso$3.50`. Fixing that exposed a second, subtler version: the
adapter used block layout on the assumption that "HTML collapses the newline
back to a space". **Marko strips whitespace between elements in a template**, so
it does not. The rule is now that whitespace containing a newline is formatting
and whitespace without one is content — which is exactly the distinction the
author made by writing them on one line.

**Only the browser found the second one.** The template-level test asserted the
template, and the template was fine. `docs/RISK_QUEUE.md` records the pattern:
a check that inspects the artifact one layer above the defect will pass.

**An ADR claimed a verification that had not happened.** ADR-0017's first
version said every mapping row was verified against a running Marko; the
parameter row was written from memory and the build rejected it. The correction
is recorded in the ADR itself.
